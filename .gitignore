# Python が自動生成する一時ファイル（使い捨てなので履歴に入れない）
__pycache__/
*.pyc

# _trash は「削除の退避先」なので履歴管理の対象外にする
_trash/

# --- 秘密情報・個人設定をGitHubに上げないための予防線（多重防御）---
# Claude Code がフォルダ内に作る個人設定（万一作られても履歴に入れない）
.claude/
# 将来APIキーなどを書くファイル（絶対に公開しない）
.env
.env.*
# 鍵ファイル全般
*.key
# 認証情報っぽいファイル全般
credentials*
